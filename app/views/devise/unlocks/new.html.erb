<% content_for :heading, "Resend unlock instructions" %>

<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "jq-auth-form" }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="jq-field">
    <%= f.label :email, "Email address", class: "jq-label" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "jq-input" %>
  </div>

  <%= f.submit "Resend unlock instructions", class: "jq-btn jq-btn--primary jq-btn--block" %>
<% end %>

<%= render "devise/shared/links" %>
